JMVC.extend('2wdb', function () {

    var bindings = {},
        ps = JMVC.Channel('2wdb');

    return {
        bind : function (obj, key, field) {
            
        },
        unbind : function () {

        }
    };
});